% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pcr_insilico.R
\name{simulate_pcr}
\alias{simulate_pcr}
\title{Simulate an in silico PCR}
\usage{
simulate_pcr(gene_seq, fwd_primer, rev_primer, max_mismatch = 2)
}
\arguments{
\item{gene_seq}{\code{DNAString} object, the complete reference sequence (not
necessarily the same variable used in \code{\link[=design_primers]{design_primers()}}; it can be
any sequence on which you want to test the primer pair).}

\item{fwd_primer}{Character string or \code{DNAString} with the forward
primer (5' -> 3').}

\item{rev_primer}{Character string or \code{DNAString} with the reverse
primer (5' -> 3').}

\item{max_mismatch}{Maximum number of mismatches allowed when searching
for additional binding sites, to assess specificity (default \code{2}). The
amplicon itself is always computed from the exact match.}
}
\value{
A list of class \code{"rflp_amplicon"} with:
\item{amplicon}{\code{DNAString} object with the amplicon sequence.}
\item{start, end}{1-based coordinates in \code{gene_seq}.}
\item{size}{Amplicon size in bp.}
\item{n_sites_fwd, n_sites_rev}{Number of binding sites found allowing
\code{max_mismatch} mismatches; values greater than 1 suggest possible
non-specific binding in the reference sequence used.}
}
\description{
Given a reference sequence and a primer pair, locates the binding sites
(allowing a configurable number of mismatches to assess specificity) and
extracts the resulting amplicon as a \code{DNAString} object.
}
