% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dimer_hairpin.R
\name{evaluate_hairpin}
\alias{evaluate_hairpin}
\title{Evaluate hairpin (intramolecular) structure formation}
\usage{
evaluate_hairpin(primer_seq, min_stem = 3, min_loop = 3)
}
\arguments{
\item{primer_seq}{Primer sequence (5' -> 3'), as text or \code{DNAString}.}

\item{min_stem}{Minimum stem length to consider a risk (default \code{3}).}

\item{min_loop}{Minimum loop length between the two stem arms (default
\code{3}; a real hairpin requires a minimum loop size to fold physically).}
}
\value{
A list with:
\item{stem}{Length of the longest complementary stem found.}
\item{loop}{Length of the loop associated with that stem (\code{NA} if
\code{stem == 0}).}
\item{dg_approx}{Heuristic estimate of free energy (kcal/mol; see the
note in \code{\link[=evaluate_dimer]{evaluate_dimer()}} about its limitations).}
\item{risk}{\code{TRUE} if \code{stem >= 4}, the default heuristic threshold for
flagging the hairpin as concerning.}
}
\description{
Heuristic to detect hairpin (stem-loop) structures within a single
primer: it searches for the longest pair of internal arms that are
complementary to each other (one read 5'->3' and the other 3'->5'),
separated by a loop of at least \code{min_loop} nucleotides. This structure
is not evaluated by \code{\link[=evaluate_dimer]{evaluate_dimer()}}, which only compares two
sequences against each other (intermolecular), not a sequence against
itself in a hairpin configuration (intramolecular).
}
