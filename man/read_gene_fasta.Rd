% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_gene_fasta.R
\name{read_gene_fasta}
\alias{read_gene_fasta}
\title{Read a gene sequence from a FASTA file}
\usage{
read_gene_fasta(fasta_file, seq_index = 1)
}
\arguments{
\item{fasta_file}{Path to the FASTA file (\code{.fa}/\code{.fasta}) containing the
reference sequence (e.g. the full sequence of a gene downloaded from
NCBI).}

\item{seq_index}{Integer. Index of the sequence to extract if the file
contains more than one record (default \code{1}).}
}
\value{
A \code{Biostrings::DNAString} object with the loaded sequence.
}
\description{
Loads a reference (DNA) sequence from a FASTA file and returns it as a
\code{Biostrings::DNAString} object, ready for the rest of the \code{rflpSNP}
functions.
}
\details{
If the FASTA file contains multiple sequences, the function emits an
informative message and uses only the sequence indicated by \code{seq_index}.
The input FASTA is expected to contain a single reference sequence
(e.g. the genomic region of interest), since the rest of the workflow
(\code{locate_snp()}, \code{design_primers()}, \code{simulate_pcr()}) assumes a single
reference sequence per analysis.
}
\examples{
\dontrun{
gene_seq <- read_gene_fasta("MTHFR_completeseq.fa")
}

}
