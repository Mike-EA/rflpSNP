% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pira_sites.R
\name{find_pira_sites}
\alias{find_pira_sites}
\title{Scan for viable PIRA-PCR (enzyme, mismatch) combinations at a SNP}
\usage{
find_pira_sites(
  gene_seq,
  snp_pos,
  ref_allele,
  alt_allele,
  enzyme_panel = pira_enzyme_panel(),
  flank_length = 12,
  gap = 0,
  min_dist_3prime = 2,
  sides = c("upstream", "downstream")
)
}
\arguments{
\item{gene_seq}{\code{DNAString} (or plain character) with the reference
sequence.}

\item{snp_pos}{1-based coordinate of the SNP in \code{gene_seq}.}

\item{ref_allele, alt_allele}{The two alleles at the SNP position
(single-character strings, on the same strand as \code{gene_seq}), e.g.
\code{"C"} and \code{"T"}.}

\item{enzyme_panel}{\code{data.frame} with columns \code{name}, \code{motif},
\code{cut_offset} (default \code{\link[=pira_enzyme_panel]{pira_enzyme_panel()}}). You can pass your own
panel with the same columns.}

\item{flank_length}{How many bases of genomic context to consider on
each side of the SNP when testing motif alignments (default \code{12};
should be at least as long as the longest motif in \code{enzyme_panel}).}

\item{gap}{Number of unmodifiable genomic bases between a primer's true
3' end and the SNP (default \code{0}: the SNP is the base immediately
following the primer, the classic PIRA-PCR geometry).}

\item{min_dist_3prime}{Minimum allowed distance (bp) between the
primer's 3' terminal base and the nearest engineered mismatch, so the
mismatch does not sit at the very last base (which risks blocking
polymerase extension). Default \code{2}.}

\item{sides}{Which side(s) of the SNP to search for the engineered
primer: \code{"upstream"} (a forward primer whose 3' end approaches the
SNP), \code{"downstream"} (a reverse primer approaching from the other
side), or both (default \code{c("upstream", "downstream")}).}
}
\value{
A \code{data.frame}, one row per viable (enzyme, side, frame)
combination, sorted with natural sites first, then by ascending
number of required mismatches, with columns: \code{enzyme}, \code{motif},
\code{cut_offset} (as given in \code{enzyme_panel}, i.e. relative to the
enzyme's own canonical orientation), \code{search_motif},
\code{search_cut_offset} (the forward-strand representation to use when
verifying the site on an actual amplicon with
\code{\link[=find_restriction_site]{find_restriction_site()}} -- identical to \code{motif}/\code{cut_offset} for
\code{side = "upstream"} or for palindromic motifs, but different for a
non-palindromic enzyme on \code{side = "downstream"}), \code{side},
\code{snp_motif_pos}, \code{n_mismatch}, \code{natural_site}, \code{cuts_on} (whether the
site forms on the \code{"ref"} or \code{"alt"} allele), \code{mismatch_dist_3prime},
\code{required_base}, \code{original_base} (the last three are comma-separated
strings when \code{n_mismatch > 1}).
}
\description{
For every enzyme in \code{enzyme_panel} and every possible alignment of its
recognition motif over the primer/SNP boundary, evaluates whether a
restriction site that discriminates the reference and alternate alleles
can be formed -- either naturally (0 mismatches, meaning ordinary
PCR-RFLP would already work and PIRA-PCR is not actually needed) or by
introducing a single engineered mismatch near the 3' end of one primer.
}
\details{
If any row has \code{natural_site = TRUE}, a standard (non-PIRA)
\code{design_primers()} + \code{find_restriction_site()} design should work for
that enzyme without engineering anything -- worth checking before
committing to a PIRA-PCR primer.

This function only identifies \emph{which} (enzyme, mismatch) combinations
are chemically viable; it does not yet build full primer pairs. Use
\code{\link[=design_pira_primers]{design_pira_primers()}} to turn a chosen row into an actual primer pair
with Tm/GC/dimer/hairpin filtering and amplicon simulation.
}
\seealso{
\code{\link[=design_pira_primers]{design_pira_primers()}}, \code{\link[=pira_enzyme_panel]{pira_enzyme_panel()}}
}
