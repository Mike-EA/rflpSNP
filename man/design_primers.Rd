% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_primers.R
\name{design_primers}
\alias{design_primers}
\title{Design primer pairs for PCR-RFLP}
\usage{
design_primers(
  gene_seq,
  snp_pos,
  upstream = 160,
  downstream = 200,
  min_distance_to_snp = 20,
  length_min = 18,
  length_max = 24,
  tm_min = 50,
  tm_max = 65,
  gc_min = 35,
  gc_max = 65,
  tm_diff_max = 5,
  amplicon_min = 150,
  amplicon_max = 300,
  min_fragment_diff = 25,
  max_small_fragment = 100,
  min_fragment_size = 40,
  max_candidates_per_strand = 40,
  n_top = 5,
  Na = 100,
  Mg = 2,
  dNTPs = 0.2,
  oligo_conc_nM = 500,
  verbose = TRUE
)
}
\arguments{
\item{gene_seq}{\code{DNAString} object with the complete gene sequence.}

\item{snp_pos}{1-based coordinate of the SNP in \code{gene_seq} (see
\code{\link[=locate_snp]{locate_snp()}}).}

\item{upstream, downstream}{Length (bp) of the primer search region
upstream/downstream of the SNP (default \code{160}/\code{200}; see
\code{\link[=define_work_region]{define_work_region()}}).}

\item{min_distance_to_snp}{Minimum distance (bp) between the SNP and the
nearest edge of each primer, to leave room for the restriction
enzyme's recognition site (default \code{20}).}

\item{length_min, length_max}{Minimum/maximum primer length in bp (default
\code{18}/\code{24}).}

\item{tm_min, tm_max}{Acceptable Tm range in degrees C (default
\code{50}/\code{65}).}

\item{gc_min, gc_max}{Acceptable \%GC range (default \code{35}/\code{65}).}

\item{tm_diff_max}{Maximum Tm difference between the forward and reverse
primer of the same pair (default \code{5}).}

\item{amplicon_min, amplicon_max}{Acceptable amplicon size range in bp
(default \code{150}/\code{300}, typical for PCR-RFLP).}

\item{min_fragment_diff}{Minimum difference (bp) between the two
restriction fragments for them to be considered distinguishable on a
gel (default \code{25}).}

\item{max_small_fragment}{Maximum size (bp) the smaller digestion
fragment may have for the pair to be considered suitable (default
\code{100}; small fragments separate better visually on the logarithmic
migration scale of an agarose gel).}

\item{min_fragment_size}{Minimum size (bp) of the smaller fragment, to
avoid fragments too small to visualize on a standard gel (default
\code{40}).}

\item{max_candidates_per_strand}{Cap on preselected candidates per strand
before generating all fwd x rev combinations, to bound execution time
(default \code{40}).}

\item{n_top}{Number of best pairs to report (default \code{5}).}

\item{Na, Mg, dNTPs, oligo_conc_nM}{PCR reaction parameters used for the Tm
calculation (see \code{\link[=calc_tm]{calc_tm()}}).}

\item{verbose}{If \code{TRUE} (default), prints progress messages and
filtering summaries at each stage.}
}
\value{
An object of class \code{"rflp_primers"} (a list) with:
\item{top}{\code{data.frame} with the \code{n_top} best pairs, including a
\code{recommended} column.}
\item{best_pair}{The best pair (first row of \code{top}).}
\item{n_candidates_forward, n_candidates_reverse}{Number of individual
candidates that passed the physicochemical filtering.}
\item{n_pairs_total}{Total number of pairs that met all criteria.}
\item{parameters}{List with the parameters used, for traceability.}
}
\description{
Generates, filters and ranks forward/reverse primer pairs around a SNP,
taking into account standard physicochemical properties (Tm, \%GC), risk
of self-dimer, heterodimer and hairpin formation, amplicon size, and the
expected resolution of the RFLP fragments on an agarose gel (so that the
SNP does not end up centered in the amplicon and the two cut fragments
are distinguishable).
}
\seealso{
\code{\link[=export_primers_txt]{export_primers_txt()}} to export the results to a text file,
\code{\link[=define_work_region]{define_work_region()}} for the earlier step of delimiting the search
region.
}
