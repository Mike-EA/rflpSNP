% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pira_enzymes.R
\name{pira_enzyme_panel}
\alias{pira_enzyme_panel}
\title{Restriction enzyme panel used by default in PIRA-PCR design}
\usage{
pira_enzyme_panel(
  source = c("full", "builtin"),
  commercial_only = TRUE,
  max_site_length = 12
)
}
\arguments{
\item{source}{\code{"full"} (default) loads the curated panel bundled with
the package (\code{inst/extdata/restriction_enzymes.csv}, several hundred
enzymes). \code{"builtin"} returns a small, hand-picked panel of ~33 common
commercially available enzymes, useful for quick examples/teaching or
as a fallback if the bundled file is unavailable for some reason
(e.g. a development install missing \verb{inst/extdata/}).}

\item{commercial_only, max_site_length}{Passed to \code{\link[=read_enzyme_panel]{read_enzyme_panel()}}
when \code{source = "full"} (see that function for defaults/meaning).}
}
\value{
A \code{data.frame} with columns \code{name}, \code{motif}, \code{cut_offset}.
}
\description{
Returns the restriction enzyme panel used as the default \code{enzyme_panel}
in \code{\link[=find_pira_sites]{find_pira_sites()}} and \code{\link[=design_pira_primers]{design_pira_primers()}}.
}
\details{
The bundled panel (\code{source = "full"}) is expected to be REBASE-derived;
see \code{inst/NOTICE.md} for attribution and licensing (REBASE data is
distributed under CC BY-NC -- non-commercial use). You can always
extend or replace either panel: build your own \code{data.frame} with the
same three columns (\code{name}, \code{motif}, \code{cut_offset}) -- \code{motif} uses IUPAC
codes (\code{N}, \code{R}, \code{Y}, etc.); \code{cut_offset} is the position of the cut
within the motif, counted from its first base (1-based), matching the
convention used by \code{\link[=find_restriction_site]{find_restriction_site()}} -- and pass it directly as
\code{enzyme_panel} to \code{\link[=find_pira_sites]{find_pira_sites()}}/\code{\link[=design_pira_primers]{design_pira_primers()}}, bypassing
this function entirely.
}
