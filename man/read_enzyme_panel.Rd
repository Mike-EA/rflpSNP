% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pira_enzymes.R
\name{read_enzyme_panel}
\alias{read_enzyme_panel}
\title{Load a custom restriction enzyme panel from a CSV file}
\usage{
read_enzyme_panel(
  path,
  name_col = "enzyme",
  motif_col = "recognition_site",
  cut_offset_col = "fst5",
  commercial_only = TRUE,
  max_site_length = 12
)
}
\arguments{
\item{path}{Path to the CSV file.}

\item{name_col, motif_col, cut_offset_col}{Column names in the CSV holding
the enzyme name, the recognition site (IUPAC), and the cut position
(counted from the first base of the site, 5' end, same convention as
REBASE/Biopython's \code{fst5}; \code{0} means the enzyme cuts before the first
base of the site). Defaults match a REBASE/Biopython-style export:
\code{"enzyme"}, \code{"recognition_site"}, \code{"fst5"}.}

\item{commercial_only}{If \code{TRUE} (default) and a \code{n_suppliers} column is
present, keep only enzymes with at least one commercial supplier
(\code{n_suppliers >= 1}).}

\item{max_site_length}{Enzymes with a recognition site longer than this
are dropped (default \code{12}); very long/degenerate sites rarely help
PIRA-PCR design and slow down the search substantially.}
}
\value{
A \code{data.frame} with columns \code{name}, \code{motif}, \code{cut_offset}, ready
to pass as \code{enzyme_panel} to \code{\link[=find_pira_sites]{find_pira_sites()}} or
\code{\link[=design_pira_primers]{design_pira_primers()}}.
}
\description{
Reads a CSV file of restriction enzymes (e.g. a REBASE-derived curated
database) and adapts it to the \code{name}/\code{motif}/\code{cut_offset} schema used
by \code{\link[=find_pira_sites]{find_pira_sites()}} and \code{\link[=design_pira_primers]{design_pira_primers()}}.
}
\examples{
\dontrun{
panel <- read_enzyme_panel("enzymes_610.csv")
find_pira_sites(gene_seq, snp_pos, "C", "T", enzyme_panel = panel)
}

}
