% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_pira_candidates.R
\name{scan_pira_candidates}
\alias{scan_pira_candidates}
\title{Scan for PIRA-PCR restriction site candidates}
\usage{
scan_pira_candidates(
  gene_seq,
  snp_pos,
  reference_allele,
  alternate_allele,
  enzymes = restriction_enzymes,
  mismatch_window = PIRA_MISMATCH_WINDOW,
  max_artificial_mismatches = 2,
  orientations = c("forward", "reverse")
)
}
\arguments{
\item{gene_seq}{\code{Biostrings::DNAString} with the reference sequence.}

\item{snp_pos}{Integer. 1-based position of the SNP in \code{gene_seq} (see
\code{\link[=locate_snp]{locate_snp()}}).}

\item{reference_allele, alternate_allele}{Single-character strings (A/C/G/T),
in \code{gene_seq}'s own forward-strand coordinate system (see
\code{\link[=resolve_pira_alleles]{resolve_pira_alleles()}}).}

\item{enzymes}{A data frame with the same structure as
\link{restriction_enzymes} (the default). Must include at least \code{enzyme},
\code{recognition_site}, and \code{cut_type}.}

\item{mismatch_window}{Integer vector of allowed artificial-mismatch
positions, counted from the primer's 3' end (the SNP position itself is
position 0 and is not part of this window). Default
\link{PIRA_MISMATCH_WINDOW} (\code{1:3}).}

\item{max_artificial_mismatches}{Integer. Maximum number of artificial
mismatches allowed in a single design. Default \code{2}.}

\item{orientations}{Character vector, any of \code{"forward"}, \code{"reverse"}.
Default both.}
}
\value{
A data frame, one row per valid candidate, with columns:
\describe{
\item{enzyme}{Enzyme name.}
\item{orientation}{\code{"forward"} or \code{"reverse"}.}
\item{site_start, site_end}{Recognition site coordinates in \code{gene_seq}.}
\item{n_artificial_mismatches}{1 or 2.}
\item{mismatch_positions}{Semicolon-separated genomic positions of the
artificial mismatches.}
\item{mismatch_bases_required}{Semicolon-separated list of the base(s)
that satisfy the enzyme's pattern at each mismatch position (a
position may have more than one valid base when the pattern is
itself an IUPAC ambiguity code; listed as e.g. \code{"A/G"}).}
\item{creates_site_for}{\code{"reference"} or \code{"alternate"} -- the allele
for which the site is completed.}
\item{cut_type, recognition_site}{Carried over from the enzyme catalog.}
}
Returns a zero-row data frame (same columns) if no candidates are found.
}
\description{
For a SNP already localized in \code{gene_seq} (see \code{\link[=locate_snp]{locate_snp()}}), searches
the restriction enzyme catalog (see \link{restriction_enzymes}) for enzymes
whose recognition site can be completed for exactly one allele (reference
or alternate) by introducing 1 or 2 artificial mismatches in a primer,
within the window defined by \link{PIRA_MISMATCH_WINDOW} relative to the SNP
(which sits at the primer's 3' terminus, ARMS-style).
}
\details{
The comparison is allele-vs-allele on the same template and the same
choice of artificial-mismatch bases (see the Fase 3 planning notes in the
package source for the full derivation). A design is only reported if
the site is completed for exactly one of the two alleles -- if both or
neither allele completes it, the site would not discriminate genotypes
and is not reported.

Coordinates are always in \code{gene_seq}'s forward-strand frame, for both
orientations. For \code{"reverse"}, the mismatch window falls downstream of
\code{snp_pos} (higher coordinates); constructing the actual reverse-primer
oligo sequence (reverse-complementing this region) is left to a later
stage of the pipeline.
}
\examples{
\dontrun{
scan_pira_candidates(gene_seq, snp_pos = 120,
                      reference_allele = "C", alternate_allele = "A")
}

}
