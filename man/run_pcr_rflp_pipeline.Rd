% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_pcr_rflp_pipeline}
\alias{run_pcr_rflp_pipeline}
\title{Run the complete end-to-end PCR-RFLP design workflow}
\usage{
run_pcr_rflp_pipeline(
  gene_seq,
  flank_seq,
  enzyme_motif = "GANTC",
  cut_offset = 1,
  export_txt = TRUE,
  output_file = "primers_PCR_RFLP_results.txt",
  ...
)
}
\arguments{
\item{gene_seq}{\code{DNAString} object with the complete gene sequence (see
\code{\link[=read_gene_fasta]{read_gene_fasta()}}).}

\item{flank_seq}{dbSNP flanking sequence (see \code{\link[=locate_snp]{locate_snp()}}).}

\item{enzyme_motif}{Restriction enzyme recognition motif (see
\code{\link[=find_restriction_site]{find_restriction_site()}}).}

\item{cut_offset}{Cut position within the motif (see
\code{\link[=find_restriction_site]{find_restriction_site()}}).}

\item{export_txt}{If \code{TRUE} (default), exports the primer report to text
via \code{\link[=export_primers_txt]{export_primers_txt()}}.}

\item{output_file}{Path of the text file to export (if
\code{export_txt = TRUE}).}

\item{...}{Additional arguments passed to \code{\link[=design_primers]{design_primers()}} (e.g.
\code{tm_min}, \code{amplicon_max}, \code{min_fragment_diff}, etc.).}
}
\value{
A list with the elements \code{snp}, \code{design}, \code{pcr_result} and
\code{restriction_result}, i.e. the result of each intermediate stage.
}
\description{
Convenience function that chains \code{\link[=locate_snp]{locate_snp()}}, \code{\link[=design_primers]{design_primers()}},
\code{\link[=simulate_pcr]{simulate_pcr()}}, \code{\link[=find_restriction_site]{find_restriction_site()}} and, optionally,
\code{\link[=export_primers_txt]{export_primers_txt()}}, using the best primer pair found. Intended for a
quick first pass; for teaching purposes, also running each function
separately is recommended so that students understand each stage of the
design.
}
