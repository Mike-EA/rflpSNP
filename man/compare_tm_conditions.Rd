% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tm_utils.R
\name{compare_tm_conditions}
\alias{compare_tm_conditions}
\title{Compare the Tm of a primer under different condition profiles}
\usage{
compare_tm_conditions(
  seq_dna,
  profiles = NULL,
  salt_corr_method = "Owczarzy2004"
)
}
\arguments{
\item{seq_dna}{Primer sequence (character or \code{DNAString}).}

\item{profiles}{Named list of profiles to compare. Each profile is a list
with \code{Na}, \code{Mg}, \code{dNTPs} and \code{oligo_conc_nM}. If \code{NULL} (default),
three reference profiles are used (see Details).}

\item{salt_corr_method}{Salt-correction formula to use for all profiles
(default \code{"Owczarzy2004"}).}
}
\value{
A \code{data.frame} with columns \code{profile}, \code{Na}, \code{Mg}, \code{dNTPs},
\code{oligo_nM} and \code{tm}, one row per profile.
}
\description{
Calculates the Tm of the same primer under several typical salt/oligo
condition profiles, to help understand how much of a discrepancy with
another tool (SnapGene, IDT OligoAnalyzer, etc.) is explained by the
assumed conditions rather than by the method itself.
}
\details{
Default profiles included:
\describe{
\item{\code{typical_PCR}}{\verb{Na=100, Mg=2, dNTPs=0.2, oligo=500 nM} - the
default used by \code{\link[=calc_tm]{calc_tm()}} and \code{\link[=design_primers]{design_primers()}}; represents a PCR
with a standard buffer (with Mg2+).}
\item{\code{no_Mg2}}{\verb{Na=50, Mg=0, dNTPs=0, oligo=250 nM} - approximates the
default condition of several online calculators when no PCR buffer
with Mg2+ is specified.}
\item{\code{low_salt}}{\verb{Na=50, Mg=0, dNTPs=0, oligo=50 nM}.}
}
These profiles are approximations for classroom exploration. For an
exact numeric comparison against a specific tool, set \code{Na}, \code{Mg},
\code{dNTPs} and \code{oligo_conc_nM} to match exactly the salt/oligo values
configured in that tool (not just the calculation method).
}
\examples{
\dontrun{
compare_tm_conditions("TGGTCTCTTCATCCCTCGCCTTGAA")
}

}
\seealso{
\code{\link[=calc_tm]{calc_tm()}}
}
