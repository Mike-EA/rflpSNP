% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pira_design.R
\name{design_pira_primers}
\alias{design_pira_primers}
\title{Design a full PIRA-PCR primer pair}
\usage{
design_pira_primers(
  gene_seq,
  snp_pos,
  ref_allele,
  alt_allele,
  sites = NULL,
  enzyme_panel = pira_enzyme_panel(),
  max_sites_to_try = 60,
  gap = 0,
  min_dist_3prime = 2,
  search_flank = 400,
  length_min = 18,
  length_max = 24,
  tm_min = 50,
  tm_max = 65,
  gc_min = 35,
  gc_max = 65,
  tm_diff_max = 5,
  amplicon_min = 150,
  amplicon_max = 300,
  min_fragment_diff = 25,
  max_small_fragment = 100,
  min_fragment_size = 10,
  Na = 100,
  Mg = 2,
  dNTPs = 0.2,
  oligo_conc_nM = 500,
  n_top = 5,
  verbose = TRUE
)
}
\arguments{
\item{gene_seq}{\code{DNAString} (or character) with the reference sequence.}

\item{snp_pos}{1-based coordinate of the SNP in \code{gene_seq}.}

\item{ref_allele, alt_allele}{The two alleles at the SNP position
(single-character strings), e.g. \code{"C"} and \code{"T"}.}

\item{sites}{\code{data.frame} from \code{\link[=find_pira_sites]{find_pira_sites()}} describing which
(enzyme, side, frame) combinations to try, in priority order. If
\code{NULL} (default), it is computed internally by calling
\code{\link[=find_pira_sites]{find_pira_sites()}} with \code{enzyme_panel}, \code{gap} and \code{min_dist_3prime}.}

\item{enzyme_panel}{Enzyme panel used when \code{sites = NULL} (default
\code{\link[=pira_enzyme_panel]{pira_enzyme_panel()}}).}

\item{max_sites_to_try}{How many of the top rows of \code{sites} to attempt
building a full primer pair for (default \code{60}). Not every chemically
viable (enzyme, frame) combination will yield a pair passing the
Tm/GC/amplicon/dimer filters, so trying more than one is recommended.}

\item{gap, min_dist_3prime}{Passed to \code{\link[=find_pira_sites]{find_pira_sites()}} when
\code{sites = NULL} (see that function).}

\item{search_flank}{Outer safety cap (bp) on how far to search for the
normal primer on the side opposite the engineered one (default \code{400}).
The actual search region is narrowed further, and automatically,
based on \code{amplicon_min}/\code{amplicon_max} and the engineered primer's own
genomic span -- \code{search_flank} only guards against searching
arbitrarily far if you set a very large \code{amplicon_max}. Lower it to
deliberately restrict the search distance for other reasons (e.g. to
avoid a distant repeat region you already know about).}

\item{length_min, length_max}{Primer length range in bp (default
\code{18}/\code{24}).}

\item{tm_min, tm_max, gc_min, gc_max, tm_diff_max}{Same meaning as in
\code{\link[=design_primers]{design_primers()}}.}

\item{amplicon_min, amplicon_max}{Acceptable amplicon size range in bp
(default \code{150}/\code{300}).}

\item{min_fragment_diff, max_small_fragment, min_fragment_size}{Same
meaning as in \code{\link[=design_primers]{design_primers()}}, but applied to the fragment sizes
actually observed after simulating digestion of both allele amplicons
(see Details). In PIRA-PCR the cut site sits immediately next to the
engineered primer's 3' end, so one fragment is systematically small
(roughly the engineered primer's length plus the enzyme's cut offset)
-- this is expected for the technique, not a design flaw, and the
achievable size is often dictated by which primer lengths satisfy
\code{tm_min}/\code{tm_max} rather than being freely choosable. For that reason
\code{min_fragment_size} here is a true detectability floor (default \code{10};
below this a fragment is not realistically callable on any gel), not a
comfort threshold -- designs with a small fragment above the floor but
still on the small side are not rejected, only ranked lower via \code{score}
(see \code{small_fragment} in the returned \code{top}/\code{best_pair}). Raise
\code{min_fragment_size} if your gel setup needs a larger minimum.}

\item{Na, Mg, dNTPs, oligo_conc_nM}{PCR reaction parameters used for Tm
(see \code{\link[=calc_tm]{calc_tm()}}). Note the Tm of the engineered primer is
necessarily approximate, since it is computed as if the primer were a
perfect match to itself; the true duplex against the genomic template
contains an intentional mismatch. Verify with a dedicated tool (e.g.
IDT OligoAnalyzer, entering the mismatch explicitly) before ordering
synthesis.}

\item{n_top}{Number of best pairs to report (default \code{5}).}

\item{verbose}{If \code{TRUE} (default), prints progress messages.}
}
\value{
An object of class \code{"rflp_pira_primers"} with \code{top}, \code{best_pair},
\code{n_pairs_total} and \code{sites_tried}, analogous to the object returned by
\code{\link[=design_primers]{design_primers()}}, plus PIRA-specific columns: \code{enzyme}, \code{side},
\code{n_mismatch}, \code{cuts_on}, \code{n_incidental_sites} (extra, SNP-unrelated
cut sites elsewhere in the amplicon -- present regardless of genotype,
so they don't confound genotyping but add extra bands), \code{small_fragment}
(the smaller of the two digestion fragments), \code{engineered_primer},
\code{mismatch_positions_from_3prime}, \code{required_base}, \code{original_base}.
}
\description{
Builds complete PIRA-PCR (mismatch PCR-RFLP) primer pairs: one primer
carries a deliberately introduced mismatch near its 3' end so that,
combined with the SNP itself, it creates a restriction site that
discriminates the reference and alternate alleles. The other primer is a
normal, perfectly-matching primer found with the same candidate
generation and filtering machinery used by \code{\link[=design_primers]{design_primers()}}.
}
\details{
For every candidate pair, both allele amplicons are built explicitly
(substituting the engineered primer's own sequence into its genomic
span, and the requested allele at the SNP position) and passed through
\code{\link[=find_restriction_site]{find_restriction_site()}} for real -- the cut site and fragment sizes
are verified, not just assumed from the frame geometry. A pair is
rejected if the site does not appear on the intended allele, or if it
unexpectedly appears on both (which would mean it no longer
discriminates the two alleles).
}
\seealso{
\code{\link[=find_pira_sites]{find_pira_sites()}}, \code{\link[=design_primers]{design_primers()}}, \code{\link[=simulate_pcr]{simulate_pcr()}},
\code{\link[=find_restriction_site]{find_restriction_site()}}
}
