% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_work_region.R
\name{define_work_region}
\alias{define_work_region}
\title{Define the working region around the SNP}
\usage{
define_work_region(
  gene_seq,
  snp_pos,
  upstream = 160,
  downstream = 200,
  max_region_size = 500
)
}
\arguments{
\item{gene_seq}{\code{DNAString} object with the complete gene sequence.}

\item{snp_pos}{1-based coordinate of the SNP within \code{gene_seq}, as
returned by \code{\link[=locate_snp]{locate_snp()}}.}

\item{upstream}{Number of bp to include upstream of the SNP (default
\code{160}).}

\item{downstream}{Number of bp to include downstream of the SNP (default
\code{200}).}

\item{max_region_size}{Recommended maximum size (bp) for the working
region in a standard PCR-RFLP assay (default \code{500}). A warning is
issued if exceeded; execution is not stopped, since in some cases
(genes with few primer options) it may be necessary.}
}
\value{
A list with:
\item{start_region, end_region}{1-based coordinates in \code{gene_seq}.}
\item{sequence_region}{\code{DNAString} object of the subregion.}
\item{snp_pos}{The SNP coordinate, for convenience in downstream
functions.}
}
\description{
Given the SNP coordinate, delimits the subregion of the gene sequence
within which candidate primers will be searched. The region is built
\strong{asymmetrically} with respect to the SNP (different lengths upstream
and downstream) so that, after digestion with the restriction enzyme,
the two resulting fragments have clearly distinguishable sizes on an
agarose gel (the whole point of a PCR-RFLP assay is being able to tell
genotypes apart by their band pattern).
}
