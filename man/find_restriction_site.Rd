% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restriction_site.R
\name{find_restriction_site}
\alias{find_restriction_site}
\title{Find a restriction enzyme site in the amplicon}
\usage{
find_restriction_site(amplicon, enzyme_motif = "GANTC", cut_offset = 1)
}
\arguments{
\item{amplicon}{\code{DNAString} object with the amplicon sequence (see
\code{\link[=simulate_pcr]{simulate_pcr()}}), or the full \code{"rflp_amplicon"} object returned by
that function.}

\item{enzyme_motif}{Character string with the recognition motif, in
IUPAC code (default \code{"GANTC"}, the HinfI site).}

\item{cut_offset}{Position of the cut within the motif, counted from the
motif's first nucleotide (1-based). Default \code{1}, which corresponds to
HinfI's cut pattern (\code{G^ANTC}, cutting between positions 1 and 2 of
the motif). Adjust according to the enzyme used (consult REBASE or the
supplier's technical sheet).}
}
\value{
A list with:
\item{sites}{\code{data.frame} with columns \code{start}, \code{end} and \code{cut_pos}
(coordinates within the amplicon, 1-based).}
\item{n_sites}{Number of sites found.}
}
\description{
Searches for a restriction enzyme recognition motif (e.g. \code{"GANTC"} for
HinfI) within the amplicon sequence, allowing IUPAC ambiguous bases
(\code{N}, \code{R}, \code{Y}, etc.).
}
