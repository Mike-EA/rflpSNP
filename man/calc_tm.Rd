% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tm_utils.R
\name{calc_tm}
\alias{calc_tm}
\title{Calculate the melting temperature (Tm) of an oligonucleotide}
\usage{
calc_tm(
  seq_dna,
  Na = 100,
  Mg = 2,
  dNTPs = 0.2,
  oligo_conc_nM = 500,
  nn_table = "DNA_NN_SantaLucia_2004",
  salt_corr_method = "Owczarzy2004"
)
}
\arguments{
\item{seq_dna}{Oligonucleotide sequence, as a \code{DNAString} or character
string.}

\item{Na}{Na+ concentration in mM (default \code{100}).}

\item{Mg}{Mg2+ concentration in mM (default \code{2}).}

\item{dNTPs}{Total dNTP concentration in mM (default \code{0.2}).}

\item{oligo_conc_nM}{Oligo concentration in nM (default \code{500}).}

\item{nn_table}{Nearest-neighbor thermodynamic table to use (default
\code{"DNA_NN_SantaLucia_2004"}).}

\item{salt_corr_method}{Salt-correction formula to use (default
\code{"Owczarzy2004"}, appropriate for PCR with Mg2+ and dNTPs present). See
\code{\link[=compare_tm_conditions]{compare_tm_conditions()}} to compare the effect of different
conditions on the same primer.}
}
\value{
The Tm in degrees Celsius (numeric, rounded to 1 decimal), or
\code{NA} if the calculation could not be performed.
}
\description{
Calculates the Tm of a primer using the nearest-neighbor method
(Nearest-Neighbor) via the \code{TmCalculator} package, with salt correction.
}
\section{Why the Tm calculated here may differ from SnapGene / IDT OligoAnalyzer}{
It is normal and expected for different tools to report different Tm
values for the same primer, even when all of them use the
nearest-neighbor method. The most common causes, roughly in order of
typical impact:
\enumerate{
\item \strong{Different salt conditions.} This package defaults to a realistic
PCR (\code{Na = 100}, \code{Mg = 2}, \code{dNTPs = 0.2}), while many online
calculators default to a condition without Mg2+ (e.g. \code{Na = 50},
\code{Mg = 0}). Mg2+ stabilizes the duplex much more per mM than Na+, so
its mere presence can raise the Tm by several degrees. \strong{This is by
far the most likely reason \code{rflpSNP} reports a higher Tm}: it is not
necessarily an error, but a more realistic salt condition for a PCR
with a standard buffer (which always contains Mg2+). For a fair
comparison, enter exactly the same Na+, Mg2+, dNTP and oligo
concentration values in both tools.
\item \strong{Assumed oligo concentration.} A more concentrated oligo
(higher \code{oligo_conc_nM}) predicts a slightly higher Tm. IDT
OligoAnalyzer defaults to 0.25 uM (250 nM); the default here is
500 nM (closer to a primer's concentration in an already-prepared
PCR mix).
\item \strong{Salt-correction formula.} \code{Owczarzy2004} (used here) and other
formulas (\code{Owczarzy2008}, \code{SantaLucia1998}, etc.) do not give exactly
the same result, even with identical concentrations.
\item \strong{Nearest-neighbor thermodynamic parameter table.} SantaLucia 2004,
SantaLucia & Hicks 1998, or Sugimoto 1996 differ slightly in their
enthalpy/entropy values per dinucleotide.
}

\code{TmCalculator::tm_calculate()} has also changed its interface across
package versions (for example, the salt-correction argument has been
called \code{salt_method} in some versions and \code{salt_corr_method} in others,
and the output object has moved from simple lists to \code{GRanges} objects).
This function first tries the argument name current on CRAN
(\code{salt_corr_method}) and, if that fails, retries with the name used in
earlier versions (\code{salt_method}) before extracting the Tm value with a
generic extractor that tolerates both output formats. Run
\code{\link[=check_tm_backend]{check_tm_backend()}} once to diagnose your installation if you are
unsure which version is active.
}

\seealso{
\code{\link[=check_tm_backend]{check_tm_backend()}}, \code{\link[=compare_tm_conditions]{compare_tm_conditions()}}
}
